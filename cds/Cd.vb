Public Class Cd
    Public nId As Integer
    Public sTitle As String
    Public sArtist As String
    Sub New(ByVal nId As Integer, ByVal sTitle As String, ByVal sArtist As String)
        Me.nId = nId
        Me.sTitle = sTitle
        Me.sArtist = sArtist
    End Sub
End Class